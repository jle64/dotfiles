# Make sure that environment is loaded
. ~/.profile

# Load any supplementary scripts
for config in ${XDG_CONFIG_HOME}/shell.d/*.sh ; do
    . "${config}"
done
unset -v config

if [ -f "${HOME}/.sh_local" ]; then
	. "${HOME}/.sh_local"
fi

